# Runtime
pandas>=2.2
yfinance>=0.2.40          # primary market data source (no API key needed)
google-genai>=1.0         # AI strategic report (GEMINI_API_KEY)
# Alpha Vantage and Finnhub fallbacks use the standard library only.

# Needed whenever DATABASE_URL points at Postgres or Supabase. It used to be
# commented out because the local SQLite mode does not need a driver — which
# meant the image could not talk to Postgres at all, and the container started
# and died on the first server deploy. A few megabytes is cheaper than that.
psycopg[binary]>=3.2

# Dashboard: only needed to run `serve`. The CLI works without them.
fastapi>=0.115
uvicorn[standard]>=0.32

# Verifies Supabase JWTs. Only the multi-user mode uses it, but that mode is
# the hosted one, so the image ships with it rather than failing at sign-in.
pyjwt>=2.9

# Development
pytest>=8.0
pytest-cov>=5.0
httpx>=0.27              # fastapi TestClient

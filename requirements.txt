# Runtime
pandas>=2.2
yfinance>=0.2.40          # primary market data source (no API key needed)
google-genai>=1.0         # AI strategic report (GEMINI_API_KEY)
# Alpha Vantage and Finnhub fallbacks use the standard library only.

# Optional: only needed when DATABASE_URL points at Postgres or Supabase.
# The local SQLite mode has no database driver to install.
# psycopg[binary]>=3.2

# Dashboard: only needed to run `serve`. The CLI works without them.
fastapi>=0.115
uvicorn[standard]>=0.32

# Optional: only for the Supabase multi-user mode (verifies its JWTs).
# pyjwt>=2.9

# Development
pytest>=8.0
pytest-cov>=5.0
httpx>=0.27              # fastapi TestClient
pyjwt>=2.9               # required to test the account isolation guarantee
